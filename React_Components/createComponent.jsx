function Button(){
    return (
        <div>
            <button>MY BUTTON</button>
        </div>
    )
}
// - its a component for button
// - component names must start with capital
// - its a reusable button
// - A component return JSX
export default Button // A component should be exported